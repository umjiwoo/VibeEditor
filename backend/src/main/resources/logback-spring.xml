<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 공통 콘솔 appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                [%d{yyyy-MM-dd HH:mm:ss.SS,Asia/Seoul}] [%-5level] [%thread] [%mdc{requestId}] %logger{36} - %msg%n
            </pattern>
        </encoder>
    </appender>

    <!-- 개발 환경: 콘솔 로그만 사용 -->
    <springProfile name="default, local">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- 운영 환경: JSON 로그 파일 저장 -->
    <springProfile name="prod">
        <!-- 로그 파일 디렉토리 설정 -->
        <property name="LOG_PATH" value="/var/log/vibe"/>

        <!-- JSON 형식의 파일 로그 appender -->
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_PATH}/vibe.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>${LOG_PATH}/vibe-%d{yyyy-MM-dd}.log.gz</fileNamePattern>
                <maxHistory>50</maxHistory>
            </rollingPolicy>
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <fieldNames>
                    <timestamp>@timestamp</timestamp>
                    <level>level</level>
                    <thread>thread</thread>
                    <logger>logger</logger>
                    <message>message</message>
                    <context>context</context>
                </fieldNames>
            </encoder>
        </appender>

        <!-- JSON 로그 파일로 출력 -->
        <root level="INFO">
            <appender-ref ref="FILE"/>
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

</configuration>